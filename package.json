{
  "name": "root",
  "private": true,
  "workspaces": [
    "@e2e/*",
    "@packages/*",
    "@shared/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.5"
  },
  "scripts": {
    "bootstrap": "yarn run clean && lerna run build && npm install -w @packages",
    "coverage": "lerna run coverage",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "lint-fix": "lerna run lint-fix",
    "start": "lerna run start",
    "test": "lerna run test"
  }
}
