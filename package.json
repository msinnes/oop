{
  "name": "root",
  "private": true,
  "workspaces": [
    "@e2e/*",
    "@packages/*",
    "@shared/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.5"
  },
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "yarn run clean && npm install -w @packages && yarn run build && npm install -w @e2e && yarn run build",
    "coverage": "lerna run coverage",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "lint-fix": "lerna run lint-fix",
    "start": "lerna run start",
    "test": "lerna run test"
  }
}
